{
  "name": "root",
  "private": true,
  "workspaces": [
    "app",
    "client"
  ],
  "scripts": {
    "watch:app": "npx lerna exec --scope=app npm run watch"
  },
  "devDependencies": {
    "lerna": "^7.3.0"
  }
}
